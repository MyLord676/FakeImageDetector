# FakeImageDetector
Runs an API for image processing: accepts an image (supports JPEG, PNG, etc.), processes it using a pre-trained segmentation model, and returns a binary mask of the same dimensions highlighting "suspicious" areas (e.g., defects, anomalies, potential issues in the image).
